# madar-ids: identifiers and their formats the backend and the POS core share
# (the canonical phone, order refs, the member card token).
[package]
name = "madar-ids"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
