# madar-units: inventory unit families and conversion, the backend's rule the
# POS core runs too.
[package]
name = "madar-units"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
